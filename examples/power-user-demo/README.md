# Power-user demo

Full demo for `tanstack-router-cache`.

Most apps do not need this much code. This app exists to show heavier workflows: retained forms, filtered lists, cache controls, route lifecycle state, and window scroll restoration.

```sh
bun install
bun run dev
```

Deploy this example after the `tanstack-router-cache` version in `package.json` has been published to npm.
